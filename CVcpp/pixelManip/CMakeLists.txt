cmake_minimum_required(VERSION 2.8)
project( PixelManip )
find_package(OpenCV)
include_directories(${OpenCV_INCLUDE_DIRS})
add_executable(PixelManip pixel.cpp)
target_link_libraries(PixelManip ${OpenCV_LIBS})
